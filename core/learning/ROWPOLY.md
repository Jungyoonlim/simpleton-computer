# What's Row Polymorphism?

A trick from type theory for dealing with records (objects with fields) or variants (tagged unions) in a flexible, compositional way. We describe records or variants as rows, extensible collections of labeled fields. 
